CREATE TABLE ASIGNATURA (ID BIGINT NOT NULL, CREDITOS INTEGER, NOMBRE VARCHAR, PRIMARY KEY (ID))
CREATE TABLE ESTUDIANTE (ID BIGINT NOT NULL, EMAIL VARCHAR, NOMBRE VARCHAR, PRIMARY KEY (ID))
CREATE TABLE CALIFICACION (FECHA_ULTIMO_EXAMEN DATE, NOTA DOUBLE, ASIGNATURA BIGINT NOT NULL, ESTUDIANTE BIGINT NOT NULL, PRIMARY KEY (ASIGNATURA, ESTUDIANTE))
ALTER TABLE CALIFICACION ADD CONSTRAINT FK_CALIFICACION_ESTUDIANTE FOREIGN KEY (ESTUDIANTE) REFERENCES ESTUDIANTE (ID)
ALTER TABLE CALIFICACION ADD CONSTRAINT FK_CALIFICACION_ASIGNATURA FOREIGN KEY (ASIGNATURA) REFERENCES ASIGNATURA (ID)
